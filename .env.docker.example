# Скопируй в .env рядом с docker-compose.yml при необходимости
# URL, с которого браузер ходит в API (порт как у notareon-server, по умолчанию 3000)
VITE_API_URL=http://localhost:3000
FRONTEND_PORT=5173
