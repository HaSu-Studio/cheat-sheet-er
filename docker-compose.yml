name: ${COMPOSE_PROJECT_NAME}

services:
  frontend:
    image: ${FRONTEND_IMAGE}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
    ports:
      - "${FRONTEND_PORT}:80"
